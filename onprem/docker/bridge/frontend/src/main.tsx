import React from "react";
import ReactDOM from "react-dom/client";
import LangflowBridgeClient from "./LangflowBridgeClient";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <LangflowBridgeClient />
  </React.StrictMode>
);
