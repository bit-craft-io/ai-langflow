{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; if [[ \"$f\" == *\"/server/firecrawl/\"* ]]; then echo '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"ask\",\"permissionDecisionReason\":\"server/firecrawl/ is vendored third-party Firecrawl code - confirm this edit is intentional.\"}}'; fi; }"
          }
        ]
      }
    ]
  }
}
